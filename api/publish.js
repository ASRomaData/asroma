export default async function handler(req,res){if(req.method!=='POST')return res.status(405).json({error:'POST only'});const p=process.env.PUBLISH_SECRET;if(p&&req.headers.authorization?.replace(/^Bearer\s+/i,'')!==p)return res.status(401).json({error:'Unauthorized'});return res.status(501).json({error:'Publisher endpoint placeholder: configure X/Instagram credentials and enable publishing.'});}